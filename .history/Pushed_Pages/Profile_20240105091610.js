import React from "react";
import { ScrollView, View } from "react-native";

const Profile = () => {

    return <ScrollView>
        <View>
            
        </View>
    </ScrollView>
}

export default Profile