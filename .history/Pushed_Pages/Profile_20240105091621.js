import React from "react";
import { ScrollView, View } from "react-native";

const Profile = () => {

    return <ScrollView>
        <View>
            <T
        </View>
    </ScrollView>
}

export default Profile