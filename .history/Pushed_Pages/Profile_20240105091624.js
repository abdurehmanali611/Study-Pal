import React from "react";
import { ScrollView, View } from "react-native";

const Profile = () => {

    return <ScrollView>
        <View>
            <Text></Text>
        </View>
    </ScrollView>
}

export default Profile